{
  "name": "aim-client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "serve": "browserify index.js > bundle.js && http-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ross Grambo",
  "license": "ISC",
  "dependencies": {
    "request": "^2.79.0"
  }
}
